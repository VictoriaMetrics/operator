spec:
  validation:
    openAPIV3Schema:
      properties:
        spec:
          properties:
            containers:
              items:
                properties:
                  ports:
                    items:
                      required:
                        - protocol
                        - containerPort
            initContainers:
              items:
                properties:
                  ports:
                    items:
                      required:
                        - protocol
                        - containerPort